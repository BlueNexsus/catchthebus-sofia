{
  "scripts": {
    "dev": "concurrently -k -n server,web -c green,cyan \"npm --prefix server run dev\" \"npm --prefix catchthebus-web run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
